#include "main.h"

/**
*int _islower(int c) - enter point.
*_islower funcion is greate.
*'c' parameter is int.
*Description: when c is lowercase return 1, otherwise return 0
*Return: 1 is lowercase, 0 otherwise
*/


int _islower(int c)
{

if (c >= 'a' && c <= 'z')
{
return (1);
}
else
{
return (0);
}


}
